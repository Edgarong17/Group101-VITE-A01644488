export const shoppingList: string[] = ['milk', 'bread', 'eggs'];

interface ITask {
    taskId: string;
    assign: string;
    priority: number;
    team: string;
    storyPoints: number;
}

export const taskData: ITask = {
    taskId: "1",
    assign: "Edgar",
    priority: 2,
    team: "11",
    storyPoints: 5
}